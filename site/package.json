{
  "name": "news-barge-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prestart": "ln -sf ../content content",
    "prebuild": "ln -sf ../content content",
    "build": "eleventy --config=eleventy.config.js && pagefind --site _site",
    "start": "eleventy --config=eleventy.config.js --serve"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "pagefind": "^1.1.0"
  }
}
